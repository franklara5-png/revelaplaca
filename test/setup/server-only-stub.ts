// Stub de `server-only` para os testes: o guard real lança erro quando
// importado fora de um Server Component do Next. Em teste (ambiente node) ele
// é neutralizado — a lógica sob teste é toda de servidor.
export {};
